part of orange;


class TrackballControls {
  
//  Vector2 _lastPosition;
  
  TrackballControls() {
//    _lastPosition = new Vector2.zero();
  }
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
//  static final Map<String, int> STATE = {
//        "NONE": -1, "ROTATE": 0, "ZOOM": 1, "PAN": 2
//  };
//  
//  Node control;
//  double rotateSpeed = 1.0;
//  double zoomSpeed = 1.2;
//  double panSpeed = 0.3;
//  bool noRotate = false;
//  bool noZoom = false;
//  bool noPan = false;
//  bool noRoll = false;
//  bool staticMoving = false;
//  double dynamicDampingFactor = 0.2;
//  double minDistance = 0.0;
//  double maxDistance = double.INFINITY;
//  List<int> keys = [65 /*A*/, 83 /*S*/, 68 /*D*/];
//  Vector3 target = new Vector3.zero();
//  Vector3 _lastPosition = new Vector3.zero();
//  int _state = STATE["NONE"];
//  int _preState = STATE["NONE"];
//  Vector3 _eye = new Vector3.zero();
//  Vector3 _rotateStart = new Vector3.zero();
//  Vector3 _rotateEnd = new Vector3.zero();
//  Vector3 _zoomStart = new Vector3.zero();
//  Vector3 _zoomEnd = new Vector3.zero();
//  Vector3 _panStart = new Vector3.zero();
//  Vector3 _panEnd = new Vector3.zero();
//  math.Rectangle _screen = new math.Rectangle(0, 0, 0, 0);
//  html.Element domElement;
// 
//  handleResize() {
//    _screen = domElement.getBoundingClientRect();
//  }
//  
//  getMouseOnScreen(num clientX, num clientY) {
//    return new Vector2(
//        (clientX - _screen.left) / _screen.width,
//        (clientY - _screen.top) / _screen.height);
//  }
//  
//  getMouseProjectionOnBall(clientX, clientY) {
//    var mouseOnBall = new Vector3(
//        (clientX - _screen.width * 0.5 - _screen.left) / (_screen.width * 0.5),
//        (_screen.height * 0.5 + _screen.top - clientY) / (_screen.height * 0.5),
//        0.0);
//
//    var length = mouseOnBall.length;
//    if(noRoll) {
//      if(length < math.SQRT1_2) {
//        mouseOnBall.z = math.sqrt(1.0 - length * length);
//      } else {
//        mouseOnBall.z = 0.5 / length;
//      }
//    } else if (length > 1.0){
//      mouseOnBall.normalize();
//    } else {
//      mouseOnBall.z = math.sqrt(1.0 - length * length);
//    }
//    _eye = control.position.clone();
//    _eye.sub(target);
//    var projection = WORLD_UP.clone().length = mouseOnBall.y;
//    
//    WORLD_UP.clone().splat(arg);
//  }
  
  
}















